<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cst2335.finalproject"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:debuggable="true"
        android:extractNativeLibs="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:parentActivityName="CovidSearchResult"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:testOnly="true"
        android:theme="@style/Theme.FinalProject"
        android:usesCleartextTraffic="true"
        tools:ignore="HardcodedDebugMode">
        <activity android:name=".recipeSearch.RecipePage"></activity>
        <activity android:name=".TMFavoritesActivity" />
        <activity android:name=".TMDetailActivity" />
        <activity android:name=".TMActivity" />
        <activity android:name=".CovidDBSave" />
        <activity android:name=".CovidDBLoad" />
        <activity android:name=".CovidEmptyActivity" />
        <activity android:name=".CovidSearchResult" />
        <activity android:name=".CovidMain" />
        <activity android:name=".recipeSearch.RecipeSearch" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>